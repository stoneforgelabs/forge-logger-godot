# docs/

`demo.gif` (embedded by the root `README.md`) — 26.7 s, 1280×720, 5.5 MB:
game + bug → F8 report popup → submit → dashboard report detail with AI
summary → end card.

> **Note:** the 0–12 s game segment is a stylized mock (the report popup
> faithfully mirrors the real addon UI), not real Godot footage. Worth
> re-shooting with an actual Godot game for credibility before a big push —
> the GIF renders from `main`, so swapping it later needs no re-tag.
> Regeneration scripts live in the monorepo under `marketing-assets/tools/`.

`assets/` — AssetLib form assets, referenced by raw.githubusercontent URLs:
`icon.png` (256×256) and 4 preview PNGs (1600×1000): in-game popup, reports
list, report detail with AI summary, dashboard.
